

(defn log [x] (console.log x))

(def lawyer (require "lawyer"))
(def stylus (require "stylus"))

(def )




;(defn docpad [x] (require "docpad" x))
;(docpad.createInstance)
;(log (try (raise exception)))
;(defn log [x & rest] (rest.reduce (fn [eerst x] (.log console x)) x))

(log "abc")
(def foo)
(def bar)
(log :beep-bop)

(log \f\o\o)

;(log (.checkLicenses jurist "../node_modules/docpad/"))

(.log console "a" "b" "c")


