// Generated by CoffeeScript 1.6.2
(function() {
  var exec, spawn, terminal, _ref;

  _ref = require('child_process'), exec = _ref.exec, spawn = _ref.spawn;

  exec("npm i -g nesh --save", function(err, res) {
    if (err) {
      return process.exit(1);
    }
  });

  terminal = spawn('xterm');

  terminal.stdout.on("data", function(data) {
    terminal.stdin.write("nesh -c");
    console.log("stdout: " + data);
    return console.log("Executing the 'nesh' command from the new terminal:");
  });

  terminal.on("exit", function(code) {
    return console.log("child process exited with code " + code);
  });

  setTimeout((function() {
    console.log("Timed out...");
    return terminal.stdin.end();
  }), 1000);

}).call(this);
